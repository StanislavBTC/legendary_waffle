def read_set_data_conf() -> None:
    """
    Reads data from start config and setup values
    """
    # NO CONF(DEV:SETUPED DEV VALUES)
    color_theme = "dark" # light - dark and maybe etc.
    return color_theme
